<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Malla Psicopedagogía</title>
  <style>
    body { font-family: Arial; background: #fce4ec; color: #4a148c; text-align: center; }
    .malla { display: grid; grid-template-columns: repeat(4, 1fr); gap: 10px; padding: 20px; }
    .ramo { background: #f8bbd0; padding: 15px; border-radius: 10px; cursor: pointer; transition: 0.3s; }
    .ramo:hover { background: #f06292; color: white; }
    .aprobado { background: #ce93d8 !important; text-decoration: line-through; }
  </style>
</head>
<body>
  <h1>Malla Psicopedagogía IPCHILE</h1>
  <div class="malla">
    <div class="ramo" onclick="toggle(this)">Psicología del Desarrollo</div>
    <div class="ramo" onclick="toggle(this)">Bases del Aprendizaje</div>
    <div class="ramo" onclick="toggle(this)">Neurociencia</div>
    <div class="ramo" onclick="toggle(this)">Educación Inclusiva</div>
    <!-- Agrega más ramos según tu avance -->
  </div>

  <script>
    function toggle(element) {
      element.classList.toggle("aprobado");
    }
  </script>
</body>
</html>
